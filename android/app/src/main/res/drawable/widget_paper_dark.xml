<?xml version="1.0" encoding="utf-8"?>
<!-- v1.15 — widget_paper for the dark palette. Same page and margin-rule
     geometry as widget_paper.xml; only the colours differ, taken from the app's
     own [data-mode="dark"] tokens (modes.css) so the widget matches the app. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#1E1B15" />
            <stroke android:width="1dp" android:color="#332E24" />
            <corners android:radius="18dp" />
        </shape>
    </item>
    <item
        android:left="26dp"
        android:top="13dp"
        android:bottom="13dp"
        android:width="1dp"
        android:gravity="left|fill_vertical">
        <shape android:shape="rectangle">
            <solid android:color="#8A4A42" />
        </shape>
    </item>
</layer-list>
